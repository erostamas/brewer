sudo: required
dist: trusty

ddons:
  apt:
    packages:
      - gcc-4.8
      - g++-4.8
      - libboost-all-dev

language: cpp

compiler:
  - gcc

before_script:
  - mkdir build
  - cd build
  - cmake ../

script: make brewer